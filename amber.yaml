# Amber Runtime configuration
# https://github.com/amber-runtime/playground

name: amber

app: example_customer_app.main:app
worker: example_customer_app.main:agent_runtime
path_prefix: /api

# Optional: infrastructure settings (sensible defaults applied)
# region: us-east-1
# environment: dev
# dashboard: true
